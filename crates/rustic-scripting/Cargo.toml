[package]
name = "rustic-scripting"
version = "0.1.0"
edition = "2021"

[dependencies]
mlua = { version = "0.10", features = ["luajit", "vendored"] }
log = "0.4"
rustic-hscript = { workspace = true }
